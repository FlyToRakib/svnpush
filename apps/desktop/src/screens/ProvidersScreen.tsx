import { ScreenHeader } from "../components/ScreenHeader";
import { S } from "../strings";

/** The Providers screen. */
export function ProvidersScreen() {
  return <ScreenHeader title={S.providers.title} subtitle={S.providers.subtitle} />;
}
