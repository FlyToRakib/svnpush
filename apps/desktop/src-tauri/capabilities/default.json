{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The main window: core events and window basics only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
