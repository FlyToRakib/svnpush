{
  "name": "svnpush",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "apps/desktop"
  ],
  "scripts": {
    "gen:bindings": "cargo test --quiet --workspace export_bindings",
    "dev": "npm run tauri --workspace apps/desktop -- dev",
    "build": "npm run tauri --workspace apps/desktop -- build",
    "lint": "npm run gen:bindings && npm run lint --workspace apps/desktop",
    "format": "npm run format --workspace apps/desktop",
    "typecheck": "npm run gen:bindings && npm run typecheck --workspace apps/desktop",
    "test": "npm run gen:bindings && npm run test --workspace apps/desktop"
  },
  "engines": {
    "node": "^22.22.2 || ^24.15.0 || >=26.0.0"
  }
}
